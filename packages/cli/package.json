{
  "name": "@kdx-kit/cli",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "bin": { "kdx": "dist/index.js" },
  "scripts": {
    "build": "tsup src/index.ts --format esm --dts --clean",
    "dev": "tsup src/index.ts --format esm --dts --watch",
    "lint": "eslint src --ext .ts",
    "test": "vitest --coverage",
    "type-check": "tsc --noEmit",
    "clean": "rimraf dist"
  },
  "dependencies": {
    "commander": "^12.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tsup": "^8.0.0",
    "vitest": "^1.6.0",
    "eslint": "^8.0.0",
    "@types/node": "^20.0.0",
    "rimraf": "^5.0.0"
  }
}